services:
  backend:
    image: xiaole6324/meow-arch-goat:0.1.0
    container_name: meow-arch-goat-backend
    ports:
      - 8080:8080
    environment:
      - SERVER_PORT=8080
      - DATABASE_URL=postgresql://codemao_forum:123456@host.docker.internal:5432/codemao_forum
      - RUST_LOG=INFO