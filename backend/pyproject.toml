[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi>=0.141.1",
    "sqlmodel>=0.0.39",
]

[dependency-groups]
dev = [
    "ruff>=0.16.2",
    "ty>=0.0.71",
]
